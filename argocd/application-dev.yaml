apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: firearms-search-dev
  namespace: argocd
spec:
  project: default
  
  source:
    repoURL: git@github.com:mimyrtek/firearms-search.git
    targetRevision: HEAD
    path: kustomize/overlays/dev
  
  destination:
    server: https://kubernetes.default.svc
    namespace: firearms-search
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
